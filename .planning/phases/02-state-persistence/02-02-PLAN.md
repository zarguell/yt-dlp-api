---
phase: 02-state-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - state.py
  - tests/test_state.py
  - main.py
autonomous: false

must_haves:
  truths:
    - "All unit tests for State class pass"
    - "Type checking passes for state.py"
    - "Linting passes for state.py and main.py"
    - "Coverage remains at 75%+ for State-related code"
    - "No circular import errors"
  artifacts:
    - path: "test output"
      provides: "Test results showing all State tests pass"
      contains: "PASSED"
  key_links:
    - from: "state.py"
      to: "tests/test_state.py"
      via: "import and test execution"
      pattern: "pytest.*test_state"
</key_links>
---

<objective>
Run comprehensive test suite to verify that the State class extraction is complete and all functionality works correctly.

Purpose: Validate that refactoring didn't break any existing functionality and that the code quality standards are maintained.
Output: Passing test suite with maintained coverage and code quality
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-state-persistence/02-01-SUMMARY.md
@state.py
@tests/test_state.py
</context>

<tasks>

<task type="auto">
  <name>Install development dependencies</name>
  <files>requirements.txt, requirements-test.txt</files>
  <action>
Install all required dependencies for testing and development:

```bash
pip install -r requirements.txt -r requirements-test.txt
```

This ensures that:
- uvicorn, fastapi, yt-dlp (production dependencies)
- pytest, pytest-asyncio, httpx (test dependencies)
- ruff, mypy (linting/type checking)
- coverage (coverage reporting)

Wait for installation to complete before proceeding.
  </action>
  <verify>
pip list | grep -E "(fastapi|pytest|ruff|mypy|coverage)"  # Should show installed packages
  </verify>
  <done>
All dependencies installed successfully
  </done>
</task>

<task type="auto">
  <name>Run type checking with mypy</name>
  <files>state.py, main.py</files>
  <action>
Run mypy type checker on state.py and related files:

```bash
python -m mypy state.py main.py models.py utils.py --strict
```

Or use the make target if it exists:
```bash
make typecheck
```

Expected result: No type errors in state.py
- Type hints should be complete
- Return types should match
- Optional types should be properly handled

If type errors are found, fix them in state.py:
- Add missing type hints
- Fix incorrect type annotations
- Handle Optional types correctly
  </action>
  <verify>
python -m mypy state.py --no-error-summary 2>&1 | grep -c "error:" || echo "0"  # Should be 0 errors
  </verify>
  <done>
mypy type checking passes with no errors for state.py
  </done>
</task>

<task type="auto">
  <name>Run linting with ruff</name>
  <files>state.py, main.py</files>
  <action>
Run ruff linter on state.py and main.py:

```bash
ruff check state.py main.py
```

Or use the make target if it exists:
```bash
make lint
```

Expected result: No linting errors
- Code follows Python style guidelines
- No unused imports
- No undefined variables
- Proper formatting

If linting errors are found, fix them:
- Remove unused imports
- Fix undefined variables
- Apply auto-fixes with `ruff check --fix`
  </action>
  <verify>
ruff check state.py main.py --output-format=json 2>&1 | jq '. | length' 2>/dev/null || echo "0"  # Should be 0 errors
  </verify>
  <done>
ruff linting passes with no errors
  </done>
</task>

<task type="auto">
  <name>Run State unit tests</name>
  <files>tests/test_state.py</files>
  <action>
Run all State-related unit tests:

```bash
python -m pytest tests/test_state.py -v
```

Expected results:
- All tests should pass
- Tests cover State class functionality:
  - __init__: Database initialization
  - add_task: Task creation with directory setup
  - get_task: Task retrieval
  - update_task: Status/result/error updates
  - list_tasks: Listing all tasks
  - Database persistence across State instances

Test coverage areas:
- [ ] Database initialization (_init_db)
- [ ] Task loading from database (_load_tasks)
- [ ] Task saving to database (_save_task)
- [ ] Task CRUD operations
- [ ] In-memory cache synchronization
- [ ] Directory creation and security validation
- [ ] Error handling and logging

If any tests fail:
1. Review test output to understand failure
2. Check state.py implementation for bugs
3. Fix issues in state.py
4. Re-run tests until all pass
  </action>
  <verify>
python -m pytest tests/test_state.py -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | grep -c "PASSED"  # Should match total test count (all pass)
  </verify>
  <done>
All State unit tests pass (expected: 10+ tests)
  </done>
</task>

<task type="auto">
  <name>Run full test suite</name>
  <files>tests/</files>
  <action>
Run complete test suite to ensure State extraction didn't break anything:

```bash
python -m pytest tests/ -v
```

Or use make target:
```bash
make test
```

Expected results:
- All 166+ tests should pass
- No regressions in other areas (API endpoints, utils, config)

Key test files to verify:
- tests/test_state.py: State class tests
- tests/test_api.py: API endpoint tests (may use state)
- tests/test_utils.py: Utility function tests
- tests/test_config.py: Configuration tests
- tests/test_retry.py: Retry logic tests

If tests fail:
1. Identify which test file failed
2. Check if failure is related to State import
3. Verify main.py state instantiation is correct
4. Fix any issues
5. Re-run until all pass
  </action>
  <verify>
python -m pytest tests/ -v --tb=short 2>&1 | tail -5  # Should show summary with all passed
  </verify>
  <done>
Full test suite passes (166+ tests)
  </done>
</task>

<task type="auto">
  <name>Run tests with coverage report</name>
  <files>tests/</files>
  <action>
Run test suite with coverage tracking:

```bash
python -m pytest tests/ --cov=. --cov-report=term-missing --cov-report=html
```

Or use make target:
```bash
make test-cov
```

Expected results:
- Overall coverage: 75%+ (maintained from baseline)
- state.py coverage: 80%+ (newly extracted module should be well-tested)
- No significant drop in coverage from baseline

Review coverage report:
- Identify any uncovered lines in state.py
- Ensure critical paths are tested (database operations, error handling)
- If coverage is low, add tests for uncovered areas

Coverage areas to check:
- State.__init__: Should be tested
- State._init_db: Should be tested
- State._load_tasks: Should be tested
- State._save_task: Should be tested
- State.add_task: Should be tested
- State.get_task: Should be tested
- State.update_task: Should be tested
- State.list_tasks: Should be tested
  </action>
  <verify>
python -m pytest tests/ --cov=state --cov-report=term-missing 2>&1 | grep "TOTAL" | grep -oE "[0-9]+%"  # Should show 80%+
  </verify>
  <done>
Coverage maintained at 75%+ overall, state.py at 80%+
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete State module extraction and verification</what-built>
  <how-to-verify>
Manual verification steps:

1. **Check module structure**:
   ```bash
   ls -la state.py  # Verify file exists
   head -20 state.py  # Check imports and class docstring
   ```

2. **Verify no State class in main.py**:
   ```bash
   grep -n "^class State:" main.py  # Should return nothing
   ```

3. **Review test results**:
   - Check that all 166+ tests passed
   - Verify coverage report shows 75%+ overall
   - Confirm state.py coverage is 80%+

4. **Verify import structure**:
   ```bash
   grep "from state import State" main.py tests/test_state.py  # Both should import from state
   ```

5. **Check for circular imports** (should be none):
   ```bash
   python -c "import main; print('No circular import errors')"  # Should execute without errors
   ```

Expected outcome:
- [ ] state.py file exists with State class
- [ ] main.py imports State from state.py
- [ ] main.py has no State class definition
- [ ] All tests pass
- [ ] Coverage maintained
- [ ] No circular imports
  </how-to-verify>
  <resume-signal>Type "approved" if all verification checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Final verification commands:

```bash
# 1. Verify module structure
test -f state.py && echo "state.py exists" || echo "ERROR: state.py missing"

# 2. Verify import structure
grep "from state import State" main.py && echo "main.py imports State" || echo "ERROR: Import missing"
grep "from state import State" tests/test_state.py && echo "test_state.py imports State" || echo "ERROR: Test import missing"

# 3. Verify no duplicate State class
! grep "^class State:" main.py && echo "No State class in main.py" || echo "ERROR: State class still in main.py"

# 4. Final test run
python -m pytest tests/ -q 2>&1 | tail -1
```

Expected output:
- All files exist
- Imports are correct
- No duplicate definitions
- Tests pass summary
</verification>

<success_criteria>
- Type checking passes (mypy)
- Linting passes (ruff)
- All 166+ unit tests pass
- Coverage maintained at 75%+ overall
- state.py coverage at 80%+
- No circular import errors
- Human verification approves
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-persistence/02-02-SUMMARY.md`
</output>
