---
phase: 02-state-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - state.py
  - main.py
  - tests/test_state.py
autonomous: true

must_haves:
  truths:
    - "State class is extracted from main.py into state.py"
    - "State class provides all CRUD operations for tasks"
    - "SQLite database schema is initialized correctly"
    - "In-memory cache is synchronized with database"
    - "All State class methods have proper type hints"
    - "Error handling and logging is implemented"
  artifacts:
    - path: "state.py"
      provides: "State class with database operations"
      min_lines: 150
      contains: ["class State", "def _init_db", "def _load_tasks", "def _save_task", "def add_task", "def get_task", "def update_task", "def list_tasks"]
    - path: "main.py"
      provides: "FastAPI application"
      contains: "from state import State"
      excludes: "^class State:"
  key_links:
    - from: "state.py"
      to: "models.py"
      via: "from models import JobType, Task"
      pattern: "from models import"
    - from: "state.py"
      to: "utils.py"
      via: "from utils import resolve_task_base_dir"
      pattern: "from utils import"
    - from: "main.py"
      to: "state.py"
      via: "from state import State"
      pattern: "from state import"
    - from: "tests/test_state.py"
      to: "state.py"
      via: "from state import State"
      pattern: "from state import"
---

<objective>
Validate that the State class has been properly extracted from main.py into state.py with all required functionality intact.

Purpose: Ensure the database layer is cleanly separated from the FastAPI application, maintaining all existing functionality while improving code organization.
Output: Verified state.py module with complete State class implementation
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@main.py
@models.py
@utils.py
</context>

<tasks>

<task type="auto">
  <name>Validate state.py module structure</name>
  <files>state.py</files>
  <action>
Verify that state.py contains a complete State class implementation with:

1. **Required imports** (lines 1-14):
   - datetime, json, logging, sqlite3, time, uuid
   - typing (Any)
   - fastapi (HTTPException)
   - models (JobType, Task)
   - utils (resolve_task_base_dir)

2. **Class definition** (line 17):
   - `class State:` with proper docstring

3. **Required methods** with signatures:
   - `__init__(self, db_file: str = "tasks.db", logger: logging.Logger | None = None)`
   - `_init_db(self) -> None`
   - `_load_tasks(self) -> None`
   - `_save_task(self, task: Task) -> None`
   - `add_task(self, job_type: JobType, url: str, base_output_path: str, fmt: str) -> str`
   - `get_task(self, task_id: str) -> Task | None`
   - `update_task(self, task_id: str, status: str, result: dict[str, Any] | None = None, error: str | None = None) -> None`
   - `list_tasks(self) -> list[Task]`

4. **Database schema** in `_init_db`:
   - CREATE TABLE IF NOT EXISTS tasks with columns:
     - id (TEXT PRIMARY KEY)
     - job_type (TEXT NOT NULL)
     - url (TEXT NOT NULL)
     - base_output_path (TEXT NOT NULL)
     - task_output_path (TEXT NOT NULL)
     - format (TEXT NOT NULL)
     - status (TEXT NOT NULL)
     - result (TEXT)
     - error (TEXT)
     - timestamp (TEXT NOT NULL)

5. **Instance variables**:
   - `self.tasks: dict[str, Task]`
   - `self.db_file: str`
   - `self.logger: logging.Logger`

Checklist:
- [ ] All required imports present
- [ ] Class properly defined with docstring
- [ ] All 8 required methods present with correct signatures
- [ ] Database schema matches specification
- [ ] Type hints present on all methods
- [ ] Logging statements present (logger.info, logger.debug, logger.error, logger.warning)

If any element is missing or incorrect, add it to state.py following the existing code patterns.
  </action>
  <verify>
grep -c "class State" state.py  # Should output 1
grep -c "def " state.py  # Should be at least 8 (methods)
python -c "import ast; tree = ast.parse(open('state.py').read()); methods = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; print(f'Methods: {len([m for m in methods if not m.startswith(\"__\")])}')" 2>/dev/null || echo "AST check skipped"
  </verify>
  <done>
State class in state.py contains all required methods with proper signatures, type hints, and database schema
  </done>
</task>

<task type="auto">
  <name>Verify main.py imports from state.py</name>
  <files>main.py</files>
  <action>
Verify that main.py has been updated to import State from state.py:

1. **Import statement** (around line 42):
   ```python
   from state import State
   ```

2. **State class removed**:
   - Ensure main.py does NOT contain `class State:` definition
   - Search for any remaining State class definition in main.py

3. **State instantiation** (around line 216):
   ```python
   state = State(logger=logger)
   ```

Checklist:
- [ ] `from state import State` present
- [ ] No `class State:` definition in main.py
- [ ] `state = State(logger=logger)` instantiation present after logger initialization

If import is missing, add it after the models import section (after line 41).
If State class still exists in main.py, remove it (it should only be in state.py).
  </action>
  <verify>
grep "from state import State" main.py  # Should find the import
! grep "^class State:" main.py  # Should NOT find State class definition (exit code 1 means success)
grep "state = State" main.py  # Should find instantiation
  </verify>
  <done>
main.py imports State from state.py and no longer contains State class definition
  </done>
</task>

<task type="auto">
  <name>Verify test imports and structure</name>
  <files>tests/test_state.py</files>
  <action>
Verify that tests correctly import State from the state module:

1. **Check imports** in tests/test_state.py:
   - Should have: `from state import State`
   - Should NOT have: `from main import State`

2. **Verify test structure**:
   - TestState class should exist
   - Tests should use State class through normal instantiation (not via main.state)

Checklist:
- [ ] test_state.py imports from state module
- [ ] No imports of State from main module
- [ ] Test fixtures create State instances directly

If test_state.py imports State from main, update it to import from state.
  </action>
  <verify>
grep "from state import State" tests/test_state.py  # Should find import
! grep "from main import.*State" tests/test_state.py  # Should NOT import from main (exit code 1 means success)
  </verify>
  <done>
tests/test_state.py imports State from state.py, not from main.py
  </done>
</task>

</tasks>

<verification>
Run static analysis to verify the module structure:

```bash
# Verify state.py module exists and is importable (structure check only)
python -c "import ast; ast.parse(open('state.py').read()); print('state.py: Syntax OK')"

# Verify main.py doesn't define State class
! grep "^class State:" main.py

# Count State class methods
grep -c "    def " state.py
```

Expected results:
- state.py parses without syntax errors
- No State class definition in main.py
- At least 8 methods in State class
</verification>

<success_criteria>
- state.py exists with complete State class implementation
- State class has all 8 required methods with proper type hints
- main.py imports State from state.py (not defined locally)
- tests/test_state.py imports State from state.py
- No duplicate State class definitions
- Database schema properly defined in _init_db method
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-persistence/02-01-SUMMARY.md`
</output>
