---
phase: 03-service-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - main.py
  - tests/test_api.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "main.py imports YtDlpService from service module"
    - "YtDlpService class definition removed from main.py"
    - "All 170 tests pass after refactoring"
    - "API endpoints still work with extracted service"
  artifacts:
    - path: "main.py"
      provides: "FastAPI application without YtDlpService definition"
      imports: ["from service import YtDlpService"]
      not_contains:
        - "class YtDlpService:"
        - "def get_info"
        - "def list_formats"
        - "def download_video"
        - "def download_audio"
        - "def download_subtitles"
        - "def download_subtitles_v2"
    - path: "tests/test_api.py"
      provides: "Updated tests that import YtDlpService from service"
      imports: ["from service import YtDlpService"]
  key_links:
    - from: "main.py"
      to: "service.py"
      via: "import YtDlpService"
      pattern: "from service import.*YtDlpService"
    - from: "tests/test_api.py"
      to: "service.py"
      via: "import YtDlpService"
      pattern: "from service import.*YtDlpService"
    - from: "main.py"
      to: "service.py"
      via: "service instance usage"
      pattern: "service\.(get_info|list_formats|download_video|download_audio|download_subtitles|download_subtitles_v2)"
---

<objective>
Update main.py to import YtDlpService from service.py and remove class definition

Purpose: Complete service layer extraction by updating all import references
Output: main.py using extracted YtDlpService from service module, all tests passing
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-layer/03-01-SUMMARY.md

@main.py (YtDlpService class definition and usage)
@service.py (extracted YtDlpService class)
@tests/test_api.py (endpoint tests)
</context>

<tasks>

<task type="auto">
  <name>Add import and remove YtDlpService class from main.py</name>
  <files>main.py</files>
  <action>
    Update main.py to import YtDlpService from service.py:

    1. Add import after existing imports (around line 50):
       from service import YtDlpService

    2. Remove YtDlpService class definition from main.py:
       - Delete lines 224-808 (the entire YtDlpService class)
       - This includes all methods: get_info, list_formats, download_video, download_audio, download_subtitles, _select_best_subtitle_language, _get_all_english_languages, download_subtitles_v2

    3. Keep the service instance creation at line ~808 (adjust line number after deletion):
       service = YtDlpService()

    4. Ensure no other code references need updating (service instance is used throughout)

    The key is that after deletion:
    - main.py imports YtDlpService from service
    - service instance is created the same way
    - All endpoints use service.get_info(), service.download_video(), etc. unchanged
  </action>
  <verify>
    1. grep "from service import YtDlpService" main.py returns the import
    2. grep "class YtDlpService" main.py returns no results
    3. grep "service = YtDlpService()" main.py returns the instance creation
    4. python3 -c "from main import app; print('Import successful')" exits without error
  </verify>
  <done>
    main.py imports YtDlpService from service module, class definition removed, service instance created
  </done>
</task>

<task type="auto">
  <name>Run full test suite to verify refactoring</name>
  <files>tests/</files>
  <action>
    Run the full test suite to verify YtDlpService extraction didn't break anything:

    1. Run all tests: pytest -v
    2. Verify all 170 tests pass (or expected count)
    3. Check for any test failures related to YtDlpService

    If any tests fail:
    1. Review the error message to identify the issue
    2. If it's an import issue, check that tests/import statements are correct
    3. If it's a logic issue, ensure service.py has identical implementation to original
  </action>
  <verify>
    1. pytest -v exits with exit code 0
    2. All 170 tests pass (or expected count)
    3. No test failures related to YtDlpService
  </verify>
  <done>
    All 170 tests pass, YtDlpService extraction verified
  </done>
</task>

<task type="auto">
  <name>Run type checking and linting on updated codebase</name>
  <files>main.py, service.py</files>
  <action>
    Run type checking and linting to ensure code quality:

    1. Run mypy on main.py and service.py: mypy main.py service.py
    2. Run ruff on main.py and service.py: ruff check main.py service.py
    3. If there are any errors, fix them

    Note: There may be existing mypy errors in main.py (dict type issues with yt_dlp) - these are pre-existing and don't need to be fixed in this phase. Focus on any new errors introduced by the extraction.
  </action>
  <verify>
    1. mypy main.py service.py exits (may have pre-existing errors, but no new errors)
    2. ruff check main.py service.py exits with no new errors
    3. No type errors related to YtDlpService imports
  </verify>
  <done>
    Type checking and linting complete, no new errors introduced by YtDlpService extraction
  </done>
</task>

<task type="auto">
  <name>Verify API endpoints still work</name>
  <files>main.py</files>
  <action>
    Verify API endpoints work correctly with extracted YtDlpService:

    1. Start the FastAPI application: python3 -c "from main import app; print('App loads successfully')"
    2. Verify the app routes are registered: python3 -c "from main import app; print([r.path for r in app.routes])"

    The key check is that the application loads without import errors and all routes are still present.
  </action>
  <verify>
    1. Python script prints "App loads successfully"
    2. Route list includes /info, /formats, /download, /audio, /subtitles, /v2/subtitles
    3. No import errors when loading main.py
  </verify>
  <done>
    FastAPI application loads successfully, all routes present, YtDlpService integration working
  </done>
</task>

</tasks>

<verification>
- main.py imports YtDlpService from service module
- YtDlpService class definition removed from main.py
- All 170 tests pass
- No new type checking or linting errors
- FastAPI application loads with all routes present
</verification>

<success_criteria>
YtDlpService successfully extracted to service.py, main.py updated to import from service, all tests passing, API endpoints functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-layer/03-02-SUMMARY.md`
</output>
