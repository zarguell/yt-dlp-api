---
phase: 03-service-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service.py
  - tests/test_service.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "service.py file exists with YtDlpService class"
    - "All YtDlpService methods extracted with identical signatures"
    - "service.py has necessary imports (utils, models, yt_dlp)"
    - "No circular import errors in service.py"
  artifacts:
    - path: "service.py"
      provides: "YtDlpService class wrapper around yt-dlp"
      min_lines: 700
      contains:
        - "class YtDlpService:"
        - "def get_info"
        - "def list_formats"
        - "def download_video"
        - "def download_audio"
        - "def download_subtitles"
        - "def download_subtitles_v2"
    - path: "tests/test_service.py"
      provides: "Unit tests for YtDlpService methods"
      min_lines: 50
  key_links:
    - from: "service.py"
      to: "utils.py"
      via: "import ensure_dir"
      pattern: "from utils import.*ensure_dir"
    - from: "service.py"
      to: "models.py"
      via: "import enums"
      pattern: "from models import.*EnglishMode|SubtitlePreference|SubtitleFormat"
    - from: "service.py"
      to: "yt_dlp"
      via: "import yt_dlp"
      pattern: "import yt_dlp"
---

<objective>
Extract YtDlpService class from main.py into standalone service.py module

Purpose: Separate yt-dlp wrapper logic from FastAPI application for better modularity
Output: service.py with YtDlpService class and unit tests
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-persistence/02-02-SUMMARY.md

@main.py (lines 224-808: YtDlpService class definition)
@utils.py (ensure_dir function)
@models.py (EnglishMode, SubtitlePreference, SubtitleFormat enums)
</context>

<tasks>

<task type="auto">
  <name>Create service.py with YtDlpService class</name>
  <files>service.py</files>
  <action>
    Create service.py with the following:

    1. Import statements:
       - Standard library: logging, time, re (for regex in subtitle helpers)
       - Third-party: yt_dlp
       - Local: from utils import ensure_dir; from models import EnglishMode, SubtitlePreference, SubtitleFormat; from typing import Any, Sequence, cast

    2. Copy YtDlpService class from main.py lines 224-808 to service.py:
       - get_info(url, quiet) - static method
       - list_formats(url) - static method
       - download_video(url, output_path, fmt, quiet, cookie_file) - static method
       - download_audio(url, output_path, audio_format, audio_quality, quiet, cookie_file) - static method
       - download_subtitles(...) - static method with partial success tracking
       - _select_best_subtitle_language(info, english_rank, prefer) - static helper
       - _get_all_english_languages(info, prefer) - static helper
       - download_subtitles_v2(...) - static method with policy-based selection

    3. Preserve all method signatures, docstrings, and implementation exactly

    4. Ensure logger is defined: logger = logging.getLogger("yt-dlp-api")

    Do NOT include the service = YtDlpService() instance creation in service.py
  </action>
  <verify>
    1. service.py exists with ~700+ lines
    2. grep "class YtDlpService" service.py returns class definition
    3. grep -E "def (get_info|list_formats|download_video|download_audio|download_subtitles|download_subtitles_v2)" service.py returns all 6 public methods
    4. python3 -c "import service; print('Import successful')" exits without error
  </verify>
  <done>
    service.py exists with YtDlpService class containing all methods, imports are correct, no import errors
  </done>
</task>

<task type="auto">
  <name>Create unit tests for YtDlpService</name>
  <files>tests/test_service.py</files>
  <action>
    Create tests/test_service.py with the following test structure:

    1. Import fixtures and dependencies:
       - pytest, unittest.mock.patch
       - from service import YtDlpService
       - from models import EnglishMode, SubtitlePreference, SubtitleFormat

    2. Test YtDlpService.get_info():
       - Mock yt_dlp.YoutubeDL to return fake info dict
       - Verify sanitize_info is called
       - Verify returned dict contains expected data

    3. Test YtDlpService.list_formats():
       - Mock get_info to return formats list
       - Verify formats are extracted correctly
       - Handle empty formats case

    4. Test _select_best_subtitle_language():
       - Test exact match selection (manual and auto)
       - Test regex match selection (en-US pattern)
       - Test preference modes (manual_only, auto_only, manual_then_auto)
       - Test no match found case

    5. Test _get_all_english_languages():
       - Test extracting en, en-US, en-GB variants
       - Test with manual_only preference
       - Test with auto_only preference

    Use unittest.mock.patch for yt_dlp.YoutubeDL to avoid actual network calls
  </action>
  <verify>
    1. pytest tests/test_service.py -v runs without errors
    2. All tests pass (5+ tests expected)
    3. Coverage report shows service.py methods are tested
  </verify>
  <done>
    Unit tests for YtDlpService exist and pass, mocking yt-dlp calls
  </done>
</task>

<task type="auto">
  <name>Verify service.py extraction with type checking and linting</name>
  <files>service.py</files>
  <action>
    Run type checking and linting on the new service.py module:

    1. Run mypy on service.py: mypy service.py
    2. Run ruff on service.py: ruff check service.py
    3. If there are any type errors or linting issues, fix them in service.py

    Common issues to watch for:
    - Missing type hints on method signatures
    - Unused imports
    - Incorrect Any type usage
  </action>
  <verify>
    1. mypy service.py exits with no errors (exit code 0)
    2. ruff check service.py exits with no errors
    3. service.py has proper type hints throughout
  </verify>
  <done>
    service.py passes mypy type checking and ruff linting
  </done>
</task>

</tasks>

<verification>
- service.py exists with YtDlpService class
- All methods from main.py are present in service.py
- Unit tests pass for YtDlpService methods
- Type checking and linting pass
- No circular import errors
</verification>

<success_criteria>
service.py module extracted with YtDlpService class, all methods present, unit tests passing, type checking and linting clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-layer/03-01-SUMMARY.md`
</output>
