---
phase: 03-service-layer
plan: 03
type: execute
wave: 1
depends_on: [03-01, 03-02]
files_modified: [tests/test_api.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 186 tests pass after fixing enum imports"
    - "TestSubtitlesV2Endpoints tests reference enums from models, not main"
    - "No import errors for EnglishMode, SubtitlePreference, SubtitleFormat"
  artifacts:
    - path: "tests/test_api.py"
      provides: "API endpoint integration tests"
      imports_englishmode_from: "models (not main)"
    - path: "models.py"
      provides: "EnglishMode, SubtitlePreference, SubtitleFormat enums"
      exported: true
  key_links:
    - from: "tests/test_api.py"
      to: "models.EnglishMode"
      via: "from models import EnglishMode, SubtitlePreference, SubtitleFormat"
      pattern: "assert payload_dict.*EnglishMode|SubtitlePreference|SubtitleFormat"
---

<objective>
Fix failing tests in test_api.py by updating enum imports from main to models module.

Purpose: Close verification gap where 8 SubtitlesV2 tests fail due to referencing main.EnglishMode instead of importing from models module.
Output: All 186 tests passing with corrected import structure.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-service-layer/03-01-SUMMARY.md
@.planning/phases/03-service-layer/03-02-SUMMARY.md
@.planning/phases/03-service-layer/03-service-layer-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Fix enum imports in test_api.py</name>
  <files>tests/test_api.py</files>
  <action>
    In tests/test_api.py:
    1. Add import statement: `from models import EnglishMode, SubtitlePreference, SubtitleFormat`
    2. Find all instances of `main.EnglishMode` and replace with `EnglishMode` (8 occurrences)
    3. Find all instances of `main.SubtitlePreference` and replace with `SubtitlePreference` (2 occurrences)
    4. Find all instances of `main.SubtitleFormat` and replace with `SubtitleFormat` (4 occurrences)

    The failing tests are in the TestSubtitlesV2Endpoints class (lines ~530-620):
    - test_subtitles_v2_best_one_default
    - test_subtitles_v2_all_english
    - test_subtitles_v2_explicit_mode
    - test_subtitles_v2_auto_only
    - test_subtitles_v2_manual_only
    - test_subtitles_v2_format_both
    - test_subtitles_v2_manual_then_auto
    - test_subtitles_v2_with_language_ranking

    Do NOT add these enums back to main.py - they belong in models.py as per the modularization strategy.
  </action>
  <verify>
    grep -n "main\.EnglishMode\|main\.SubtitlePreference\|main\.SubtitleFormat" tests/test_api.py
    # Should return no results

    grep -n "from models import EnglishMode" tests/test_api.py
    # Should show the new import statement
  </verify>
  <done>
    tests/test_api.py imports enums from models module, no references to main.EnglishMode remain
  </done>
</task>

<task type="auto">
  <name>Run full test suite to verify all 186 tests pass</name>
  <files>tests/test_api.py</files>
  <action>
    Run the complete test suite:
    ```bash
    pytest -v
    ```

    Verify that:
    1. All 186 tests pass (previously 178 passing, 8 failing)
    2. Specifically check that TestSubtitlesV2Endpoints tests now pass
    3. No new test failures introduced
  </action>
  <verify>
    pytest -v | grep -E "passed|failed|error"
    # Should show: 186 passed
  </verify>
  <done>
    All 186 tests pass, including the 8 previously failing SubtitlesV2 tests
  </done>
</task>

<task type="auto">
  <name>Run type checking and linting</name>
  <files>tests/test_api.py</files>
  <action>
    Verify no regressions in type checking or linting:
    ```bash
    make lint
    mypy .
    ```

    This ensures the import changes don't introduce any type checking issues or linting violations.
  </action>
  <verify>
    make lint  # Should exit 0
    mypy .     # Should show 0 errors
  </verify>
  <done>
    No linting or type checking errors introduced
  </done>
</task>

</tasks>

<verification>
- tests/test_api.py imports EnglishMode, SubtitlePreference, SubtitleFormat from models module
- No references to main.EnglishMode, main.SubtitlePreference, main.SubtitleFormat remain
- All 186 tests pass (including the 8 previously failing SubtitlesV2 tests)
- No linting or type checking errors
</verification>

<success_criteria>
All 186 tests pass with corrected enum import structure, closing the verification gap on "All 186 tests pass after refactoring"
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-layer/03-03-SUMMARY.md`
</output>
